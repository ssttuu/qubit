apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.Compute.AppName }}
  labels:
    chart: '{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}'
spec:
  ports:
  # Port that accepts gRPC and JSON/HTTP2 requests over HTTP.
  - port: 80
    targetPort: {{ .Values.Compute.ExternalPort }}
    protocol: TCP
    name: http2
  selector:
    app: {{ .Values.Compute.AppName }}
  type: LoadBalancer
