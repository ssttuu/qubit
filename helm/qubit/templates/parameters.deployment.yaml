apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Values.Parameters.AppName }}
spec:
  replicas: {{ .Values.Parameters.Replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
        maxSurge: {{ .Values.Parameters.MaxSurge }}
  template:
    metadata:
      labels:
        app: {{ .Values.Parameters.AppName }}
        version: {{ .Chart.Version }}
        release: {{ .Release.Name }}
        revision: {{ .Release.Revision | quote }}
        githash: {{ .Values.Githash }}
    spec:
      containers:
      - name: esp
        image: gcr.io/endpoints-release/endpoints-runtime:1
        args: [
          "--http_port", "{{ .Values.Parameters.ExternalHttpPort }}",
          "--http2_port", "{{ .Values.Parameters.ExternalHttp2Port }}",
          "-s", "{{ .Values.Parameters.ApiName }}",
          "-v", "{{ .Values.Parameters.ApiId }}",
          "-a", "grpc://127.0.0.1:{{ .Values.Parameters.InternalPort }}"
        ]
        ports:
          - containerPort: {{ .Values.Parameters.ExternalHttpPort }}
          - containerPort: {{ .Values.Parameters.ExternalHttp2Port }}
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 100m
            memory: 128Mi
      - name: {{ .Values.Parameters.AppName }}
        image: {{ .Values.Parameters.ImageName }}:{{ .Values.Githash }}
        ports:
          - containerPort: {{ .Values.Parameters.InternalPort }}
        env:
          - name: GOOGLE_PROJECT_ID
            value: "{{ .Values.GoogleProjectId }}"
          - name: DATASTORE_PROJECT_ID
            value: "{{ .Values.GoogleProjectId }}"
          - name: DATASTORE_DATASET
            value: "{{ .Values.DatastoreDataset }}"
          - name: GOOGLE_APPLICATION_CREDENTIALS
            value: "/opt/etc/credentials/service-account-key-file.json"
          - name: STORAGE_BUCKET
            value: "{{ .Values.Parameters.StorageBucket }}"
          - name: PORT
            value: "{{ .Values.Parameters.InternalPort }}"
        volumeMounts:
          - name: google-credentials
            mountPath: /opt/etc/credentials
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
      volumes:
        - name: google-credentials
          secret:
            secretName: google-service-account
