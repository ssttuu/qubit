apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.Server.AppName }}
  labels:
    chart: '{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}'
spec:
  ports:
  # Port that accepts gRPC and JSON/HTTP2 requests over HTTP.
  - port: 80
    targetPort: {{ .Values.Server.ExternalPort }}
    protocol: TCP
    name: http2
  selector:
    app: {{ .Values.Server.AppName }}
  type: LoadBalancer
