apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.Server.AppName }}
  labels:
    chart: '{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}'
    version: {{ .Chart.Version }}
    release: {{ .Release.Name }}
    revision: {{ .Release.Revision | quote }}
    githash: {{ .Values.Githash }}
spec:
  ports:
  # Port that accepts gRPC and JSON/HTTP2 requests over HTTP.
  - port: {{ .Values.Server.ExternalHttp2Port }}
    targetPort: {{ .Values.Server.ExternalHttp2Port }}
    protocol: TCP
    name: http2
  - port: {{ .Values.Server.ExternalHttpPort }}
    targetPort: {{ .Values.Server.ExternalHttpPort }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.Server.AppName }}
  type: LoadBalancer
