apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Values.Scenes.AppName }}
spec:
  replicas: {{ .Values.Scenes.Replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
        maxSurge: {{ .Values.Scenes.MaxSurge }}
  template:
    metadata:
      labels:
        app: {{ .Values.Scenes.AppName }}
        version: {{ .Chart.Version }}
        release: {{ .Release.Name }}
        revision: {{ .Release.Revision | quote }}
        githash: {{ .Values.Githash }}
    spec:
      containers:
      - name: esp
        image: gcr.io/endpoints-release/endpoints-runtime:1
        args: [
          "--http_port", "{{ .Values.Scenes.ExternalHttpPort }}",
          "--http2_port", "{{ .Values.Scenes.ExternalHttp2Port }}",
          "-s", "{{ .Values.Scenes.ApiName }}",
          "-v", "{{ .Values.Scenes.ApiId }}",
          "-a", "grpc://127.0.0.1:{{ .Values.Scenes.InternalPort }}"
        ]
        ports:
          - containerPort: {{ .Values.Scenes.ExternalHttpPort }}
          - containerPort: {{ .Values.Scenes.ExternalHttp2Port }}
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 100m
            memory: 128Mi
      - name: {{ .Values.Scenes.AppName }}
        image: {{ .Values.Scenes.ImageName }}:{{ .Values.Githash }}
        ports:
          - containerPort: {{ .Values.Scenes.InternalPort }}
        env:
          - name: GOOGLE_PROJECT_ID
            value: "{{ .Values.GoogleProjectId }}"
          - name: DATASTORE_PROJECT_ID
            value: "{{ .Values.GoogleProjectId }}"
          - name: DATASTORE_DATASET
            value: "{{ .Values.DatastoreDataset }}"
          - name: GOOGLE_APPLICATION_CREDENTIALS
            value: "/opt/etc/credentials/service-account-key-file.json"
          - name: STORAGE_BUCKET
            value: "{{ .Values.Scenes.StorageBucket }}"
          - name: PORT
            value: "{{ .Values.Scenes.InternalPort }}"
        volumeMounts:
          - name: google-credentials
            mountPath: /opt/etc/credentials
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
      volumes:
        - name: google-credentials
          secret:
            secretName: google-service-account
