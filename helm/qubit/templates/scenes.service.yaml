apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.Scenes.AppName }}
  labels:
    chart: '{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}'
    version: {{ .Chart.Version }}
    release: {{ .Release.Name }}
    revision: {{ .Release.Revision | quote }}
    githash: {{ .Values.Githash }}
spec:
  ports:
  # Port that accepts gRPC and JSON/HTTP2 requests over HTTP.
  - port: {{ .Values.Scenes.ExternalHttp2Port }}
    targetPort: {{ .Values.Scenes.ExternalHttp2Port }}
    protocol: TCP
    name: http2
  - port: {{ .Values.Scenes.ExternalHttpPort }}
    targetPort: {{ .Values.Scenes.ExternalHttpPort }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.Scenes.AppName }}
  type: LoadBalancer
